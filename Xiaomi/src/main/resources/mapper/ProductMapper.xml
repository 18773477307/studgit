<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 使用映射接口的方式， 命名空间的值必须与映射接口的全限定名一致 -->
<mapper namespace="com.xiaomi.mapper.ProductMapper" >
  <select id="getAllProduct" resultType="Product">
  	 select * from (select a.*,rownum rn from 
	(select p.*,g.goodsName from product p,goods g,datadict d 
	where g.goodsId=p.goodsId and p.ptformat=d.dirId order by ptId)
		 a where #{pageNo}>=rownum)b where rn>#{pageSize}
  </select>
  
  <select id="getTotalProduct" resultType="int">
  	select count(1) from product
  </select>
</mapper>